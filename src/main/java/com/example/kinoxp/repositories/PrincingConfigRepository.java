package com.example.kinoxp.repositories;

import com.example.kinoxp.model.PricingConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PrincingConfigRepository extends JpaRepository<PricingConfig, Integer> {
}
